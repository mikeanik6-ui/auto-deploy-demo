name: Auto-Deploy Test

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello from GitHub Actions!"

      - name: Simulate Deploy
        run: sleep 10 && echo "Deploying..."

      - name: Simulate Deploy
        run: sleep 15 && echo "Deploy SUCCESS!"
        exit 1 → forces error
